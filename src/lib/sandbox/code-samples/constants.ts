export { KEYCLOAK_TOKEN_URL, OPENAPI_BASE_URL } from "../sandbox-config";

export const PLACEHOLDERS = {
    clientId: "YOUR_CLIENT_ID",
    clientSecret: "YOUR_CLIENT_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
} as const;
