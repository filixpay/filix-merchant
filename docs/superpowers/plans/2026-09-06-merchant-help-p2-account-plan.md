# Merchant Help P2 — Account & Reporting Plan

> **For agentic workers:** Content + wiring only. HEAD UI is SSOT.

**Goal:** Ship `account/notifications`, `account/audit-logs`, `account/maintenance`, `account/close-account`, `payments/transaction-reports` with EN+ZH, manifest, i18n CTAs, Dashboard deep links; merge to `main`.

### Task 1: Specs — update V1 P2 status

### Task 2: Manifest + i18n labelKeys

### Task 3: `en/zh/account.ts` + register in `article-content.ts`; append transaction-reports to payments modules

### Task 4: Wire `HelpDeepLinkButton` on listed Dashboard pages

### Task 5: Verify (`tsc`, `verify:messages`, loader smoke) → merge + push
